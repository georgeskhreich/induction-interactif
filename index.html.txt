<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guide Interactif Induction - Bac Libanais</title>
    <!-- Bibliothèques React et Tailwind via CDN -->
    <script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap');
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-slate-50">
    <div id="root"></div>

    <script type="text/babel">
        const Icon = ({ children, color = "currentColor", size = 24 }) => (
            <svg xmlns="http://www.w3.org/2000/svg" width={size} height={size} viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                {children}
            </svg>
        );

        const ChevronRight = (props) => <Icon {...props}><path d="m9 18 6-6-6-6"/></Icon>;
        const ChevronLeft = (props) => <Icon {...props}><path d="m15 18-6-6 6-6"/></Icon>;
        const CheckCircle2 = (props) => <Icon {...props}><circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/></Icon>;
        const AlertCircle = (props) => <Icon {...props}><circle cx="12" cy="12" r="10"/><line x1="12" x2="12" y1="8" y2="12"/><line x1="12" x2="12.01" y1="16" y2="16"/></Icon>;
        const Info = (props) => <Icon {...props}><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></Icon>;
        const BookOpen = (props) => <Icon {...props}><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></Icon>;
        const Activity = (props) => <Icon {...props}><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></Icon>;
        const Zap = (props) => <Icon {...props}><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></Icon>;
        const MousePointer2 = (props) => <Icon {...props}><path d="m4 4 7.07 17 2.51-7.39L21 11.07z"/></Icon>;

        const App = () => {
            const [currentStep, setCurrentStep] = React.useState(0);
            const [quizAnswers, setQuizAnswers] = React.useState({});
            const [showFeedback, setShowFeedback] = React.useState(false);

            const handleQuiz = (step, value) => {
                setQuizAnswers(prev => ({ ...prev, [step]: value }));
                setShowFeedback(true);
            };

            const steps = [
                {
                    title: "1. Analyse de l'Orientation",
                    icon: <Activity className="text-blue-500" />,
                    content: (
                        <div className="space-y-4">
                            <p className="text-slate-700">Le sens positif (+) imposé sur le circuit définit le vecteur normal <b>n</b> par la règle de la main droite.</p>
                            <div className="bg-blue-50 p-4 rounded-xl border-l-4 border-blue-500 shadow-sm">
                                <h4 className="font-bold text-blue-800">Règle de la main droite</h4>
                                <p className="text-sm text-blue-900">Les doigts suivent l'orientation du circuit. Le pouce indique <b>n</b>. Ici, <b>n</b> est vers la gauche. Comme <b>B</b> est vers la droite (énoncé), l'angle θ = 180°.</p>
                            </div>
                            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                                <div className="p-4 bg-white border rounded-xl shadow-sm text-center">
                                    <div className="font-bold text-slate-500 mb-2 uppercase text-xs">Vecteurs</div>
                                    <div className="h-24 bg-slate-50 rounded flex items-center justify-center gap-10">
                                        <div className="flex flex-col items-center"><span className="font-bold text-blue-600">B →</span><div className="w-8 h-1 bg-blue-600"></div></div>
                                        <div className="flex flex-col items-center"><span className="font-bold text-red-600">← n</span><div className="w-8 h-1 bg-red-600"></div></div>
                                    </div>
                                    <p className="text-xs mt-2 text-slate-400">θ = 180° ⇒ cos(θ) = -1</p>
                                </div>
                                <div className="space-y-4">
                                    <p className="font-semibold text-slate-800">Quel est l'angle θ ?</p>
                                    <div className="grid grid-cols-1 gap-2">
                                        {['0°', '90°', '180°'].map((opt) => (
                                            <button 
                                                key={opt}
                                                onClick={() => handleQuiz('step1', opt)}
                                                className={`p-3 text-left rounded-lg border-2 transition-all ${quizAnswers.step1 === opt ? 'bg-blue-100 border-blue-500' : 'bg-white border-slate-100 hover:border-blue-200'}`}
                                            >
                                                {opt}
                                            </button>
                                        ))}
                                    </div>
                                </div>
                            </div>
                        </div>
                    )
                },
                {
                    title: "2. Flux et Loi de Faraday",
                    icon: <Zap className="text-amber-500" />,
                    content: (
                        <div className="space-y-4">
                            <div className="bg-slate-50 p-4 rounded-xl border border-slate-200 text-center">
                                <p className="font-mono text-lg font-bold">Φ = N · B · S · cos(180°) = -0,1 · B</p>
                                <p className="font-mono text-xl font-black text-rose-600 mt-2">e = -dΦ/dt = 0,1 · (dB/dt)</p>
                            </div>
                            <div className="overflow-x-auto">
                                <table className="w-full text-xs text-left border-collapse">
                                    <thead>
                                        <tr className="bg-slate-100">
                                            <th className="p-2 border">Intervalle</th>
                                            <th className="p-2 border">dB/dt (T/s)</th>
                                            <th className="p-2 border">e (V)</th>
                                            <th className="p-2 border">i (A)</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr><td className="p-2 border">[1s ; 3s]</td><td className="p-2 border">-1,0</td><td className="p-2 border">-0,10</td><td className="p-2 border text-red-600">-0,01</td></tr>
                                        <tr><td className="p-2 border">[3s ; 5s]</td><td className="p-2 border">+1,0</td><td className="p-2 border">+0,10</td><td className="p-2 border text-green-600">+0,01</td></tr>
                                        <tr><td className="p-2 border">[5s ; 9s]</td><td className="p-2 border">+0,5</td><td className="p-2 border">+0,05</td><td className="p-2 border text-green-600">+0,005</td></tr>
                                    </tbody>
                                </table>
                            </div>
                            <p className="text-[10px] text-slate-500">Note : i = e/R avec R = 10 Ω. On néglige la résistance interne du solénoïde.</p>
                        </div>
                    )
                },
                {
                    title: "3. Loi de Lenz & Oscilloscope",
                    icon: <BookOpen className="text-emerald-600" />,
                    content: (
                        <div className="space-y-4">
                            <div className="bg-emerald-50 p-4 rounded-xl border border-emerald-100">
                                <h4 className="font-bold text-emerald-800 text-sm">Justification (Bac Libanais) :</h4>
                                <p className="text-xs italic text-slate-700 mt-1">"D'après la loi de Lenz, le courant induit s'oppose par ses effets à la cause qui lui a donné naissance."</p>
                            </div>
                            <div className="flex gap-4 items-start">
                                <div className="w-1/2 p-3 bg-white border rounded-xl shadow-sm">
                                    <div className="text-xs font-bold text-slate-400 mb-2">SPOT LUMINEUX [1s-3s]</div>
                                    <div className="h-20 bg-black rounded relative overflow-hidden">
                                        <div className="absolute inset-0 flex items-center justify-center">
                                            <div className="w-1 h-1 bg-green-400 rounded-full shadow-[0_0_10px_green] translate-y-4 animate-pulse"></div>
                                        </div>
                                    </div>
                                    <p className="text-[10px] mt-2 text-center italic">i &lt; 0 ⇒ Déviation vers le bas</p>
                                </div>
                                <div className="w-1/2 p-3 bg-white border rounded-xl shadow-sm">
                                    <div className="text-xs font-bold text-slate-400 mb-2">SPOT LUMINEUX [3s-5s]</div>
                                    <div className="h-20 bg-black rounded relative overflow-hidden">
                                        <div className="absolute inset-0 flex items-center justify-center">
                                            <div className="w-1 h-1 bg-green-400 rounded-full shadow-[0_0_10px_green] -translate-y-4 animate-pulse"></div>
                                        </div>
                                    </div>
                                    <p className="text-[10px] mt-2 text-center italic">i &gt; 0 ⇒ Déviation vers le haut</p>
                                </div>
                            </div>
                        </div>
                    )
                },
                {
                    title: "4. Question 3 : Interrupteur K1",
                    icon: <Info className="text-indigo-500" />,
                    content: (
                        <div className="space-y-4">
                            <p className="text-sm">L'ouverture de quel interrupteur ne change pas l'oscillogramme ?</p>
                            <div className="p-4 bg-indigo-50 rounded-xl border border-indigo-100">
                                <h4 className="font-bold text-indigo-900">Réponse : K1</h4>
                                <p className="text-xs text-indigo-800 mt-2 leading-relaxed">
                                    L'oscilloscope est branché directement aux bornes du solénoïde. Même si <b>K1</b> est ouvert, le circuit de la résistance est coupé ($i=0$), mais la <b>f.é.m. d'induction e</b> (tension à vide) continue d'exister aux bornes de la bobine tant que le flux varie. L'oscilloscope affiche donc toujours la courbe de la f.é.m.
                                </p>
                            </div>
                        </div>
                    )
                }
            ];

            return (
                <div className="max-w-4xl mx-auto my-4 md:my-8 bg-white rounded-3xl shadow-2xl overflow-hidden border border-white">
                    <div className="p-6 md:p-8 border-b bg-white">
                        <div className="flex justify-between items-center mb-4">
                            <h1 className="text-xl md:text-2xl font-black text-slate-900">Guide Interactif d'Induction</h1>
                            <span className="text-[10px] font-bold text-blue-600 bg-blue-50 px-3 py-1 rounded-full uppercase tracking-tighter">Terminale - Bac Libanais</span>
                        </div>
                        <div className="h-2 w-full bg-slate-100 rounded-full overflow-hidden">
                            <div className="h-full bg-blue-600 transition-all duration-500" style={{ width: `${((currentStep + 1) / steps.length) * 100}%` }}></div>
                        </div>
                    </div>

                    <div className="p-6 md:p-10 min-h-[400px]">
                        <div className="flex items-center gap-4 mb-8">
                            <div className="p-3 bg-slate-50 rounded-xl">{steps[currentStep].icon}</div>
                            <h2 className="text-xl md:text-2xl font-black text-slate-800">{steps[currentStep].title}</h2>
                        </div>
                        {steps[currentStep].content}
                        
                        {currentStep === 0 && showFeedback && (
                            <div className={`mt-6 p-4 rounded-xl border flex items-center gap-3 transition-all ${quizAnswers.step1 === '180°' ? 'bg-emerald-50 text-emerald-800 border-emerald-100' : 'bg-rose-50 text-rose-800 border-rose-100'}`}>
                                {quizAnswers.step1 === '180°' ? <CheckCircle2 size={20} /> : <AlertCircle size={20} />}
                                <p className="font-bold text-sm">{quizAnswers.step1 === '180°' ? 'Correct ! θ = 180° car B et n sont de sens opposés.' : 'Réessayez. Appliquez la règle de la main droite sur le circuit.'}</p>
                            </div>
                        )}
                    </div>

                    <div className="p-6 bg-slate-50 flex justify-between items-center">
                        <button onClick={() => {setCurrentStep(Math.max(0, currentStep - 1)); setShowFeedback(false);}} disabled={currentStep === 0} className="font-bold text-slate-500 hover:text-slate-900 disabled:opacity-30 transition flex items-center gap-2">
                            <ChevronLeft size={18} /> Précédent
                        </button>
                        <button onClick={() => {setCurrentStep(Math.min(steps.length - 1, currentStep + 1)); setShowFeedback(false);}} className="px-6 md:px-10 py-3 bg-blue-600 text-white rounded-xl font-bold shadow-lg shadow-blue-200 hover:bg-blue-700 transition flex items-center gap-2">
                            {currentStep === steps.length - 1 ? 'Revoir' : 'Suivant'} {currentStep < steps.length - 1 && <ChevronRight size={18} />}
                        </button>
                    </div>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>